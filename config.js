module.exports = {
    consumer_key: "add you own credentials",
    consumer_secret: "add you own credentials",
    access_token: "add you own credentials",
    access_token_secret: "add you own credentials",
   // timeout_ms: 60 * 1000, // optional HTTP request timeout to apply to all requests.
   // strictSSL: true // optional - requires SSL certificates to be valid.
  };
  
  // You Get these credentials from AWS accounts